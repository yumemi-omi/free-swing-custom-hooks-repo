import { VFC } from 'react';
import DndComponent from '@/components/root/DndComponent';

const Dnd: VFC = () => {
  return (
    <section>
      <DndComponent />
    </section>
  );
};

export default Dnd;
